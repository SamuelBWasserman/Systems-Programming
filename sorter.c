#include <stdio.h>
#include <stdlib.h>
// #include "sorter.h"
#include <string.h>

int main(int argc, char **(argv)){
	if (argc < 2 || argc > 2) {
		printf("invalid input\n");
    		return 0;
  	}
	FILE *data_file = fopen(arv[1],"r");
}


